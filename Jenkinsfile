pipeline {
    agent { label 'agent any' }  // or "agent any" for Jenkins >= 2.0 
    stages {
        stage('Build') {
            steps {
                echo 'Hello, World from the Jenkinsfile!'
            }
        }
    }
}
